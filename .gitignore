# Internal working notes — must never be published (see "Critical Boundary" in the file itself)
CONTEXT.md

# Tracker backups. The backup carries internal columns (pertinence scores)
# that the public projection deliberately omits. The EXPORT SCRIPT is versioned
# here; its OUTPUT must never be.
backups/

# macOS
.DS_Store

# Generated caches
__pycache__/
*.pyc

# Downloaded source PDFs/articles held for reading while drafting.
# COPYRIGHTED third-party material — never publishable. The Outstanding tab
# holds the citable URL; these local copies are a reading convenience only.
source-articles/
