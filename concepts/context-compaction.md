<!--meta
category: Knowledge & Memory
short: Shrinking a conversation so it keeps fitting — and the point at which what the system knows stops being something anyone chose
aliases: [compaction, compact, context compression, conversation summarization, history truncation, sliding window, auto-compact, what happened to the earlier conversation]
tags: [Architecture, Data Governance, Model Behavior]
established: established
-->
# Context Compaction

> **Term status — Established.** A recognized term of art, in independent use beyond any single originator.

## One-line essence
Reducing a growing conversation history so it keeps fitting the model's context window — and the governance point that what gets cut, and by what rule, decides what the system still knows.

---

## Technical definition

Every call to a model carries the whole conversation. Without intervention that history grows until it exceeds the [context window](context-window.md), costs rise with every turn, and latency rises with it. Compaction reduces the history while trying to preserve what matters.

**Strategies form a ladder from gentle to aggressive**, and Microsoft's Agent Framework documents the taxonomy cleanly: collapse verbose tool results; **summarize** older spans with an LLM; keep a **sliding window** of recent turns; **truncate** oldest-first as an emergency backstop; or compose these into a pipeline that escalates only as far as the budget requires.

**The division that matters is deletion versus rewriting.** Truncation and windowing *remove* material — lossy, but every surviving line is exactly what was said. Summarization *replaces* material with new text generated by a model. **The second is lossy and generative**, which means the record can now contain sentences nobody in the conversation ever wrote, including wrong ones ([hallucination](hallucination.md)).

**Structure constrains what can be dropped.** An assistant message containing tool calls and its corresponding results form an atomic unit; removing one without the other produces an API error. Compaction therefore operates on groups, not messages, and system instructions are conventionally preserved throughout.

**The finding that should change how this is treated: compaction is not controllable, and it is not repeatable.** Cim et al. (2026) report that with LLM-based summarization **the operator has no fine-grained control over summary volume, since prompt instructions are largely ignored**, and that both the number of output tokens and *the information retained* **fluctuate substantially from run to run — making the agent's retained knowledge unpredictable**. Two identical sessions can end up remembering different things. Summarization also blocks: their measurements show it stalling agent inference for tens of seconds.

**It does not apply everywhere.** Compaction is relevant only where an agent manages its own history and re-sends it each call. Where conversation state is held server-side by the provider, the same reduction still happens — it is simply not yours to configure, inspect, or record.

---

## Plain-language version

Every time you send a message, the whole conversation goes with it. So a long session gets slower, more expensive, and eventually too big to send at all.

Compaction is how systems deal with that. There are gentler and harsher versions: collapse the noisy tool output, summarize the older part, keep only the last few exchanges, or — when nothing else is enough — just drop the oldest material.

**The important distinction is between cutting and rewriting.** Cutting loses things, but whatever survives is word-for-word what was actually said. Summarizing hands the old conversation to a model and keeps its summary instead — so the record now contains sentences nobody in the conversation ever wrote, and those sentences can be wrong.

Then there is the part most people do not expect. Researchers found you cannot really steer summarization: instructions about how much to keep are largely ignored, and **what gets retained varies from run to run**. Run the same conversation twice and the system may end up remembering different things. It is not a setting you tune; it is a behavior you inherit.

This is why "it forgot what we agreed" is often not a memory problem at all. The information was there, a compaction step decided it was not worth keeping, and nothing recorded that decision. **What the system knows was shaped by a rule nobody read, at a moment nobody logged.**

---

## AI literacy notes

1. **Deleting and summarizing are different operations** with different risks — one loses text, the other invents it.
2. **A summary is model output**, so it can be wrong in the ordinary way model output is wrong.
3. **You cannot reliably steer how much is retained** — instructions to the summarizer are largely ignored.
4. **It is not repeatable.** Retained information varies between runs, so the same session twice may know different things.
5. **Summarization is a blocking model call**, adding latency and cost to the thing meant to reduce cost.
6. **Structure limits what can be cut** — tool calls and their results move together, or the call fails.
7. **"It forgot" is often compaction, not memory** — and the two have different fixes ([memory](memory-ai-systems.md)).
8. **Where the provider manages history, compaction still happens** — you simply cannot see or configure it.

---

## Governance notes

**Core question:** When this system drops or rewrites part of a conversation, what rule decided that, and is there any record of what was removed?

**Watch for:**
- No log of compaction events, so there is no way to tell whether a gap in behavior came from a cut ([audit trail](audit-trail-ai.md))
- Summaries persisted into stored history as though they were the original record ([data provenance](data-provenance-lineage.md))
- Compaction strategy treated as a performance setting, when it determines what the system retains
- Reliance on instructions to the summarizer to preserve specific material — measurably unreliable
- Reproducibility assumed across sessions where compaction has run ([determinism vs probabilism](determinism-vs-probabilism.md))
- Regulated or consequential decisions taken after a compaction step, with no record of what was in context at the time ([compliance](compliance-ai-systems.md))
- Provider-managed conversation state where the reduction rule is neither visible nor recorded
- Personal data surviving into a summary that outlives the retention rule applied to the original ([data minimization](data-minimization.md))

**Practice:**
- **Log compaction as an event** — when it fired, which strategy, how much was removed — even when the removed content is not retained
- **Keep the pre-compaction record where a decision must be auditable**, since the summary is not evidence of what was said
- Prefer deletion over summarization where faithfulness matters more than continuity; prefer summarization where continuity matters more than fidelity — and **decide which, deliberately**
- Do not rely on prompt instructions to protect specific facts through a summarization step; carry them outside the compacted history instead
- Escalate through a pipeline — collapse tool output first, summarize second, window third, truncate last — so aggressive loss is a backstop rather than a default
- Test long-session behavior explicitly, since compaction failures only appear past the length most testing covers ([evaluation](evaluation.md))
- Where the provider manages history, **ask what the reduction rule is and record the answer**, because you are accountable for a behavior you do not control

**Key accountability owner:** whoever owns the retention and audit obligations for the conversation — because compaction is where information leaves the system without a deletion decision, and it is currently configured by engineers as a cost setting rather than by anyone treating it as a records question.

*→ [Governance & Observability Notes](../notes/governance-and-observability.md) — observability signals and cross-cutting accountability checklist.*

---

## Confidence level

**High on the mechanism and the strategy taxonomy, high on the unpredictability finding, medium on practice.** The mechanics are documented in production framework references from more than one vendor and are directly inspectable. The controllability and variance results come from named 2026 research measured across four model backbones and two benchmarks, which is stronger evidence than the practitioner impression this entry would otherwise rest on. **Weaker ground:** there is no standard for logging compaction events, no agreed practice for retaining pre-compaction records, and no published data on how often summarization loses material that later mattered — the entry's central governance claim is a structural argument, not a measured failure rate. **Strategy names and defaults vary between frameworks** and will date; the deletion-versus-rewriting distinction will not.

---

## Related concepts

- [Context Window](context-window.md) — the bound that makes compaction necessary
- [Context (AI Systems)](context-ai-systems.md) — what is being reduced, and why it is re-sent every turn
- [Memory (AI Systems)](memory-ai-systems.md) — what is deliberately retained, as against what compaction happens to spare
- [Harness Paradigm](harness-paradigm.md) — the layer where compaction is implemented and configured
- [Hallucination](hallucination.md) — the risk a generated summary inherits
- [Inference](inference.md) — where the cost and latency compaction is meant to reduce are actually paid
- [Agent Skills](agent-skills.md) — instructions loaded into the same budget compaction is defending
- [Audit Trail (AI)](audit-trail-ai.md) — the record that makes a compaction decision reviewable
- [Data Minimization](data-minimization.md) — the retention question a summary quietly reopens
- [Failure Modes (AI Systems)](failure-modes-ai-systems.md) — "it forgot" as a named, diagnosable failure

---

## Sources

| ID | Source | Contribution to this entry |
|---|---|---|
| SRC-270 | Microsoft — *Compaction* (Microsoft Agent Framework documentation, updated August 2026) · [link](https://learn.microsoft.com/en-us/agent-framework/concepts/agents/conversations/compaction) | The strategy taxonomy this entry uses — tool-result collapse, summarization, sliding window, truncation, and escalating pipelines — plus atomic message groups and the applicability limit for provider-managed conversation state. Evidence of the term in independent use: a competing vendor documenting the same concept as a first-class framework feature. ⚠️ Vendor documentation; the framework is marked experimental. |
| SRC-271 | Cim, Musa; Topcu, Burak; Das, Chita; Kandemir, Mahmut Taylan — *Parallel Context Compaction for Long-Horizon LLM Agent Serving* (2026) · [link](https://arxiv.org/abs/2605.23296) | The load-bearing finding: operators have no fine-grained control over summary volume because prompt instructions are largely ignored, and both output length and **retained information fluctuate substantially between runs**, making retained knowledge unpredictable. Also measures summarization as a blocking stall of tens of seconds. Evaluated across four model backbones and two benchmarks. ⚠️ Preprint. |
| SRC-149 | Liu, N.F.; Lin, K.; Hewitt, J.; Paranjape, A.; Bevilacqua, M.; Petroni, F.; Liang, P. — *Lost in the Middle: How Language Models Use Long Contexts* (2023) · [link](https://arxiv.org/abs/2307.03172) | Why simply enlarging the window is not a substitute for compaction — retrieval from the middle of a long context is measurably less reliable. |
| SRC-069 | Anthropic — *Effective Context Engineering for AI Agents* (2025) · [link](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents) | Practitioner treatment of the context budget as a managed resource rather than a fixed limit. ⚠️ Vendor-authored. |
| SRC-137 | Zhang, Z.; Bo, X.; Ma, C.; Li, R.; Chen, X.; Dai, Q.; Zhu, J.; Dong, Z.; Wen, J.-R. (Renmin University / Huawei) — *A Survey on the Memory Mechanism of Large Language Model based Agents* (2024) · [link](https://arxiv.org/abs/2404.13501) | The in-trial / cross-trial framing that separates compaction of a live session from durable memory. |

---

## Audience relevance

| Audience | Relevance |
|---|---|
| **Technical / Professional** | Log compaction as an event, escalate through a pipeline rather than defaulting to aggressive loss, and never rely on prompt instructions to protect facts through a summarizer. |
| **Organizational** | This is where information leaves the system without anyone making a deletion decision — currently configured as a cost setting by people who are not accountable for the records question it actually is. |
| **Client-facing** | Explains "it forgot what we agreed" accurately, and why a long conversation is not a durable record of itself. |
| **LLM-native** | Deleting keeps the words and loses the content; summarizing keeps the gist and invents the words. Retained information varies run to run, so compaction is a behavior you inherit rather than a setting you tune. |

---

*Last updated: v1.0 · September 2026*
